name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    # Użycie zmiennych zdefiniowanych w skrypcie setup_repo.sh
    env:
      IMAGE_NAME: website-argocd-k8s-githubactions-kustomize-kyverno02
      NAMESPACE: davtrokyverno02
      KUSTOMIZE_PATH: manifests/production # Ścieżka do Kustomize
      PLACEHOLDER_TAG: v1.0.0-placeholder # Używamy stałej dla czytelności

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          # W Twoim kodzie używasz GHCR_PAT, zostawiam, ale rekomendowane jest użycie GITHUB_TOKEN dla akcji repozytoryjnych.
          password: ${{ secrets.GHCR_PAT }}

      # KROK NAPRAWIAJĄCY BŁĄD BUFOROWANIA
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', 'main') }}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # --- START: KROKI NAPRAWIAJĄCE BŁĄD ImagePullBackOff & Kustomize ---

      - name: Get Short SHA Tag
        id: tag
        # Pobieramy krótki SHA (pierwsze 7 znaków) jako faktyczny tag.
        run: echo "SHA_TAG=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Configure Git
        run: |
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com

      - name: Update Kustomization with new image tag
        # 🎯 POPRAWKA: Używamy working-directory, aby przejść do katalogu Kustomize
        working-directory: ${{ env.KUSTOMIZE_PATH }}
        run: |
          # Usunięto "--path"
          kustomize edit set image ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ env.PLACEHOLDER_TAG }}=ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ steps.tag.outputs.SHA_TAG }}

      - name: Commit and Push Kustomization changes
        # Commitujemy i wypychamy zmiany. ArgoCD wykryje ten nowy commit i wdroży obraz z poprawnym tagiem.
        run: |
          git add ${{ env.KUSTOMIZE_PATH }}/kustomization.yaml
          git commit -m "chore(gitops): update ${{ env.IMAGE_NAME }} image tag to ${{ steps.tag.outputs.SHA_TAG }} [skip ci]"
          git push
      # --- KONIEC: KROKI NAPRAWIAJĄCE BŁĄD ImagePullBackOff & Kustomize ---
      - name: Output Image Tag
        run: |
          echo "Obraz został wypchnięty jako: ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ steps.tag.outputs.SHA_TAG }}"
